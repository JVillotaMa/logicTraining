"""/*
 * Escribe una función que calcule si un número dado es un número de Armstrong
 * (o también llamado narcisista).
 * Si no conoces qué es un número de Armstrong, debes buscar información
 * al respecto.
 */
"""

def Armstrong(num0, num1, num2):
    total = num0**3 + num1**3 + num2**3
    total = str(total)
    if total[0] == str(num0) and total[1] == str(num1) and total[2] == str(num2):
        return True
    else: return False

print(Armstrong(3,7,0))
